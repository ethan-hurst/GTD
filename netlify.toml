[build]
  command = "npm run build"
  publish = ".netlify"

[functions]
  directory = "netlify/functions"
  included_functions = ["analytics-track", "analytics-query", "sync-check", "sync-pull", "sync-push"]
