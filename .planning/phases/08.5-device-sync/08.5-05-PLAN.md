---
phase: 08.5-device-sync
plan: 05
type: execute
wave: 3
depends_on: ["08.5-04"]
files_modified:
  - src/routes/settings/+page.svelte
  - src/lib/components/Sidebar.svelte
  - src/lib/components/StatusBar.svelte
  - src/lib/components/mobile/MobileNav.svelte
autonomous: true

must_haves:
  truths:
    - "Settings page has Device Sync card with pair/unpair functionality"
    - "User can generate a pairing code and see it displayed for entry on another device"
    - "User can enter a pairing code from another device to link"
    - "User can unpair device from Settings"
    - "User can trigger force-sync from Settings"
    - "Sidebar shows sync status indicator with last sync time"
    - "StatusBar shows sync status on desktop"
  artifacts:
    - path: "src/routes/settings/+page.svelte"
      provides: "Device Sync card in Settings page"
      contains: "Device Sync"
    - path: "src/lib/components/Sidebar.svelte"
      provides: "Sync status indicator in sidebar footer"
      contains: "syncState"
    - path: "src/lib/components/StatusBar.svelte"
      provides: "Sync status in status bar"
      contains: "syncState"
  key_links:
    - from: "src/routes/settings/+page.svelte"
      to: "src/lib/stores/sync.svelte.ts"
      via: "imports syncState for pair/unpair/forceSync"
      pattern: "import.*sync\\.svelte"
    - from: "src/lib/components/Sidebar.svelte"
      to: "src/lib/stores/sync.svelte.ts"
      via: "imports syncState for status display"
      pattern: "import.*sync\\.svelte"
---

<objective>
Build the Device Sync UI in Settings (pairing, unpairing, force-sync) and sync status indicators in Sidebar and StatusBar.

Purpose: Users need a clear interface to pair devices, monitor sync status, and manage their sync configuration. The Settings page provides the primary interaction surface, while sidebar and status bar provide at-a-glance sync state.

Output: Updated Settings page with Device Sync card, Sidebar with sync indicator, StatusBar with sync status.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.5-device-sync/08.5-RESEARCH.md
@src/routes/settings/+page.svelte
@src/lib/components/Sidebar.svelte
@src/lib/components/StatusBar.svelte
@src/lib/components/mobile/MobileNav.svelte
@src/lib/stores/sync.svelte.ts (from Plan 04)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Device Sync card to Settings page</name>
  <files>src/routes/settings/+page.svelte</files>
  <action>
Add a Device Sync card to the Settings page. Place it after the Storage section and before the Onboarding section. Import `syncState` from `$lib/stores/sync.svelte` and `formatPairingCode` from `$lib/sync/pair`.

The Device Sync card should have these states:

**State 1: Not Paired**
- Header: "Device Sync"
- Description: "Sync your GTD data across devices. Data is encrypted end-to-end using your pairing code."
- Two sub-sections:

  a) "Start New Pair" - Button "Generate Pairing Code"
     - On click: generate code, show it large and formatted (XXX-XXX) with monospace font
     - Show instruction text: "Enter this code on your other device to link them"
     - Show a "Copy" button next to the code
     - The code display should be in a highlighted box (blue-50 bg / blue-900/20 dark)

  b) "Join Existing Pair" - Input field for entering a code from another device
     - Label: "Enter pairing code from other device"
     - Input with placeholder "XXX-XXX", text-center, monospace, uppercase transform
     - "Pair Device" button below the input
     - On submit: validate code, call syncState.pair(), show success/error toast
     - Auto-format input as user types (insert dash after 3 chars)

**State 2: Paired**
- Header: "Device Sync"
- Paired status indicator (green dot + "Paired")
- Show last sync time: "Last synced: X minutes ago" (or "Never" if never synced)
- Show sync state if actively syncing (spinner + "Syncing...")
- Show error if last sync failed (red text)
- Three actions:
  a) "Sync Now" button - calls syncState.forceSync()
  b) "Unpair Device" button (destructive styling: red/outline) - confirm dialog then syncState.unpair()
  c) If pairing code not in memory (page refreshed): "Re-enter Pairing Code" input to re-authenticate

**Pairing code re-entry UX:**
- After page refresh, syncState.hasPairingCode() returns false
- Show amber warning: "Pairing code needed for sync. Re-enter to resume syncing."
- Input field for re-entering code
- On submit: setPairingCode() and trigger sync

Match existing Settings card styling:
- `bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 rounded-xl shadow-sm hover:shadow-md transition-shadow duration-200 p-4 tablet:p-6 mt-4`
- Buttons: same style as existing Settings buttons
- Full-width card (not in the 2-column grid)

Add local state variables:
- `generatedCode = $state('')` - The generated pairing code
- `inputCode = $state('')` - User's input for joining
- `reentryCode = $state('')` - Code re-entry after refresh
- `isPairing = $state(false)` - Loading state for pairing
- `isSyncing = $state(false)` - Loading state for sync

Initialize sync state on mount: In the existing `onMount`, add `await syncState.init()`.
  </action>
  <verify>Run `npx svelte-check --tsconfig ./tsconfig.json`. No type errors. Visual inspection of Settings page layout.</verify>
  <done>Settings page has Device Sync card. Unpaired state shows generate/join options. Paired state shows sync status, force-sync, unpair. Pairing code re-entry after refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Add sync status indicators to Sidebar and StatusBar</name>
  <files>src/lib/components/Sidebar.svelte, src/lib/components/StatusBar.svelte, src/lib/components/mobile/MobileNav.svelte</files>
  <action>
**Part A: Sidebar sync indicator**

In `src/lib/components/Sidebar.svelte`:

1. Import `syncState` from `$lib/stores/sync.svelte`.

2. In the sidebar footer area (near the ThemeToggle), add a sync status indicator:
   - If not paired: Show nothing (no indicator)
   - If paired + idle: Show a small cloud icon (SVG) with green dot, tooltip "Synced X ago"
   - If paired + syncing (pulling/pushing/merging): Show cloud icon with spinning indicator
   - If paired + error: Show cloud icon with red dot, tooltip showing error

3. The indicator should be compact (inline with the footer). Use a simple SVG cloud icon (not an external dependency):
   ```html
   <!-- Cloud sync icon -->
   <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
     <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"/>
   </svg>
   ```

4. In collapsed sidebar mode: Show just the icon with colored dot (no text)
5. In expanded sidebar mode: Show icon + "Synced" or "Syncing..." text

Add a `formatSyncTime` helper function similar to formatLastSave in StatusBar.

**Part B: StatusBar sync indicator**

In `src/lib/components/StatusBar.svelte`:

1. Import `syncState` from `$lib/stores/sync.svelte`.

2. Add a sync status section between the persistence status and the storage quota:
   - If not paired: Show nothing
   - If paired: Show colored dot + sync state text:
     - Idle: blue dot + "Synced X ago" (or "Synced" if just completed)
     - Pulling: blue dot + "Pulling..."
     - Pushing: blue dot + "Pushing..."
     - Merging: blue dot + "Merging..."
     - Error: red dot + "Sync error"
   - Make the sync status clickable - clicking navigates to Settings page

3. Use a pipe separator `|` between the persistence status and sync status (matching existing StatusBar style).

**Part C: Mobile MobileNav indicator**

In `src/lib/components/mobile/MobileNav.svelte`:

1. Import `syncState` from `$lib/stores/sync.svelte`.
2. Near the settings link in the mobile nav drawer, add a small sync status indicator:
   - If paired and syncing: show a small spinning icon
   - If paired and error: show a small red dot
   - Otherwise: nothing
   - Keep it minimal - mobile nav should not be cluttered

Match all styling to the existing project patterns (gray-400/500 text, small dots, transition-colors).
  </action>
  <verify>Run `npx svelte-check --tsconfig ./tsconfig.json`. No type errors. Run `npm run build` to verify build.</verify>
  <done>Sidebar shows sync status in footer (cloud icon + colored dot). StatusBar shows sync state between persistence and quota. MobileNav shows minimal sync indicator. All indicators reactive via syncState store.</done>
</task>

</tasks>

<verification>
1. `npx svelte-check --tsconfig ./tsconfig.json` passes
2. `npm run build` completes successfully
3. Settings page shows Device Sync card
4. Unpaired state: generate code + join input visible
5. Paired state: sync status + force-sync + unpair visible
6. Pairing code displayed in XXX-XXX format
7. Sidebar footer shows sync status indicator when paired
8. StatusBar shows sync status when paired
9. All indicators update reactively based on syncState
10. Styling matches existing Settings/Sidebar/StatusBar patterns
</verification>

<success_criteria>
- Full pairing UI in Settings (generate, join, unpair)
- Sync status visible in sidebar and status bar
- Pairing code re-entry after page refresh
- Force-sync button in Settings
- All UI updates reactively from sync store
- Mobile-responsive (existing patterns followed)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-device-sync/08.5-05-SUMMARY.md`
</output>
