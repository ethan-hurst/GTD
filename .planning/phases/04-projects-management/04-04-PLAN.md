---
phase: 04-projects-management
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can create a project and see all projects in a list"
    - "User sees visual warning for projects with no next actions"
    - "User can mark a project as complete"
    - "User can move a project to someday/maybe"
    - "User can view which next actions belong to which project"
    - "Processing flow links actions to projects correctly"
    - "Sidebar shows Projects link with stalled badge"
    - "Keyboard shortcut 'p' navigates to projects"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 4: Projects Management feature.

Purpose: Confirm all project management functionality works end-to-end before marking the phase complete. This covers project CRUD, stalled detection, processing flow integration, and navigation.

Output: Verified Phase 4 or list of issues to fix
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-projects-management/04-01-SUMMARY.md
@.planning/phases/04-projects-management/04-02-SUMMARY.md
@.planning/phases/04-projects-management/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build and type verification</name>
  <files></files>
  <action>
Run the following verification steps:

1. `npx tsc --noEmit` -- Confirm zero TypeScript errors
2. `npm run build` -- Confirm production build succeeds
3. Verify all expected files exist:
   - src/lib/db/operations.ts (contains getAllProjects, getStalledProjects, completeProject, moveProjectToSomeday, addProject)
   - src/lib/stores/projects.svelte.ts (exports projectState)
   - src/routes/projects/+page.svelte
   - src/lib/components/ProjectList.svelte
   - src/lib/components/ProjectItem.svelte
   - src/lib/components/ProjectDetailPanel.svelte
4. Verify integration points:
   - ProcessingFlow.svelte contains 'select-project' step
   - ActionDetailPanel.svelte contains project dropdown (getAllProjects import)
   - Sidebar.svelte contains '/projects' link
   - +layout.svelte contains 'p' keyboard shortcut

Report any issues found. If all pass, proceed to human verification.
  </action>
  <verify>
All commands exit 0. All files and integration points confirmed present.
  </verify>
  <done>
Automated verification passes: TypeScript clean, build succeeds, all files exist, all integration points wired.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 4: Projects Management -- project CRUD, stalled detection, processing flow integration, sidebar nav, keyboard shortcuts</what-built>
  <how-to-verify>
Start the dev server (`npm run dev`) and test these scenarios:

**1. Project Creation:**
- Press 'p' to navigate to /projects (verify keyboard shortcut works)
- Verify empty state shows "No active projects" message
- Type "Launch marketing website" in the create form and submit
- Verify project appears in the list
- Create 2 more projects: "Hire new developer" and "Plan vacation"

**2. Stalled Detection:**
- All 3 projects should show yellow "No next action" badge (they have no linked actions yet)
- Sidebar should show "Projects" with a yellow badge showing "3"

**3. Processing Flow Integration:**
- Navigate to Inbox (press '/')
- Capture a new item: "Write job description"
- Process it: Yes actionable -> No (takes longer) -> "It's part of a project"
- Verify project selection step appears with your 3 projects listed
- Select "Hire new developer" project
- Assign a context and confirm
- Navigate to /projects -- "Hire new developer" should NO LONGER show stalled badge
- The other 2 projects should still show stalled badges
- Sidebar stalled count should now show "2"

**4. Action-Project Linking:**
- Navigate to /actions (press 'n')
- Find the "Write job description" action
- Expand its detail panel
- Verify the Project dropdown shows "Hire new developer" selected
- Change it to a different project and save -- verify it updates

**5. Project Completion:**
- Navigate to /projects (press 'p')
- Expand "Plan vacation" project
- Click "Complete" button
- Verify it disappears from the active projects list
- Verify toast shows "Project completed"

**6. Someday/Maybe:**
- Expand "Launch marketing website" project
- Click "Someday/Maybe" button
- Verify it disappears from active projects list
- Verify toast shows appropriate message

**7. Sidebar Navigation:**
- Verify "Projects" link appears in sidebar between contexts and settings
- Click it -- should navigate to /projects
- Verify active state styling when on /projects page

Report any issues found. If all scenarios pass, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when human verification passes all 7 test scenarios.
</verification>

<success_criteria>
- All automated checks pass (TypeScript, build, file existence)
- Human verifies all 7 scenarios work correctly
- No visual bugs, no console errors, no broken interactions
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects-management/04-04-SUMMARY.md`
</output>
