---
phase: 04-projects-management
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/routes/projects/+page.svelte
  - src/lib/components/ProjectList.svelte
  - src/lib/components/ProjectItem.svelte
  - src/lib/components/ProjectDetailPanel.svelte
autonomous: true

must_haves:
  truths:
    - "User can navigate to /projects and see a list of all active projects"
    - "User can create a new project from the projects page"
    - "User sees yellow 'No next action' badge on stalled projects"
    - "User can expand a project to see detail panel with complete and someday/maybe actions"
    - "User can mark a project complete from the detail panel"
    - "User can move a project to someday/maybe from the detail panel"
  artifacts:
    - path: "src/routes/projects/+page.svelte"
      provides: "Projects route page that loads state and renders ProjectList"
      min_lines: 8
    - path: "src/lib/components/ProjectList.svelte"
      provides: "Project list with create form, empty state, and project items"
      min_lines: 40
    - path: "src/lib/components/ProjectItem.svelte"
      provides: "Single project row with stalled badge and expand toggle"
      min_lines: 30
    - path: "src/lib/components/ProjectDetailPanel.svelte"
      provides: "Expanded detail panel with edit, complete, and someday/maybe actions"
      min_lines: 50
  key_links:
    - from: "src/routes/projects/+page.svelte"
      to: "src/lib/stores/projects.svelte.ts"
      via: "import projectState, call loadProjects onMount"
      pattern: "projectState\\.loadProjects"
    - from: "src/lib/components/ProjectList.svelte"
      to: "src/lib/stores/projects.svelte.ts"
      via: "import projectState for reactive items"
      pattern: "import.*projectState"
    - from: "src/lib/components/ProjectItem.svelte"
      to: "src/lib/stores/projects.svelte.ts"
      via: "isStalled prop derived from projectState.isStalled()"
      pattern: "isStalled"
    - from: "src/lib/components/ProjectDetailPanel.svelte"
      to: "src/lib/db/operations.ts"
      via: "import completeProject, moveProjectToSomeday, updateItem, deleteItem"
      pattern: "import.*completeProject.*from.*operations"
---

<objective>
Build the projects route and all UI components for viewing, creating, and managing GTD projects.

Purpose: This is the primary user interface for Phase 4. Users need to see all their projects, identify stalled ones, create new projects, and take actions on them (complete, move to someday/maybe). Mirrors the established /actions route pattern.

Output: /projects route page, ProjectList, ProjectItem, and ProjectDetailPanel components
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-projects-management/04-RESEARCH.md
@.planning/phases/04-projects-management/04-01-SUMMARY.md
@src/lib/stores/projects.svelte.ts
@src/lib/db/operations.ts
@src/lib/db/schema.ts
@src/routes/actions/+page.svelte
@src/lib/components/ActionList.svelte
@src/lib/components/ActionItem.svelte
@src/lib/components/ActionDetailPanel.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectItem and ProjectDetailPanel components</name>
  <files>
    src/lib/components/ProjectItem.svelte
    src/lib/components/ProjectDetailPanel.svelte
  </files>
  <action>
Create two components mirroring the ActionItem/ActionDetailPanel pattern:

**ProjectItem.svelte:**
- Props: `project: GTDItem`, `isStalled: boolean`, `isExpanded: boolean`, `onToggleExpand: () => void`
- Display project title as main text
- If `isStalled` is true, show a yellow warning badge to the right of the title with text "No next action" and a warning triangle SVG icon. Use these exact Tailwind classes: `inline-flex items-center gap-1 px-2 py-0.5 text-xs font-medium text-yellow-800 dark:text-yellow-300 bg-yellow-100 dark:bg-yellow-900/30 rounded`
- Show created date formatted with Intl.DateTimeFormat (dateStyle: 'medium') in small gray text below the title
- Clicking the row calls onToggleExpand
- If isExpanded, render ProjectDetailPanel below with slide transition (import from 'svelte/transition')
- Use the same row styling pattern as ActionItem: padding, hover states, border-bottom, rounded container
- Row background: `bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 rounded-lg` with `hover:shadow-sm transition-shadow cursor-pointer`
- Padding: `px-4 py-3`

**ProjectDetailPanel.svelte:**
- Props: `item: GTDItem`, `onSave: () => void`, `onClose: () => void`
- Mirror ActionDetailPanel structure exactly (same div wrapper, same slide transition, same header with close button)
- Editable fields: title (text input), notes (textarea), same styling as ActionDetailPanel
- Show created/modified dates in the metadata section (same format as ActionDetailPanel)
- THREE action buttons at the bottom:
  1. "Save" (blue) - calls updateItem with title/notes changes, then onSave
  2. "Complete" (green, `bg-green-600 hover:bg-green-700`) - calls completeProject(item.id), shows toast "Project completed", calls onSave
  3. "Someday/Maybe" (amber, `bg-amber-600 hover:bg-amber-700 dark:bg-amber-500`) - calls moveProjectToSomeday(item.id), shows toast "Moved to Someday/Maybe", calls onSave
  4. "Delete" (red) - same as ActionDetailPanel, confirms then deletes
- Import and use: updateItem, deleteItem, completeProject, moveProjectToSomeday from '$lib/db/operations' (note: use '$lib/db/operations' NOT relative paths in .svelte files)
- Import toast from 'svelte-5-french-toast'
- Show linked actions count: load actions for this project using getActionsByProject(item.id) in an onMount or $effect, display count like "3 next actions" or "No next actions" with appropriate color (green if >0, amber if 0)
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors. Verify both files exist and have the expected imports.
  </verify>
  <done>
ProjectItem renders project title with stalled badge and expand toggle. ProjectDetailPanel shows editable fields, action buttons (save/complete/someday/delete), and linked action count. TypeScript compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ProjectList component and /projects route page</name>
  <files>
    src/lib/components/ProjectList.svelte
    src/routes/projects/+page.svelte
  </files>
  <action>
Create the project list component and route page:

**ProjectList.svelte:**
- Import projectState from '$lib/stores/projects.svelte'
- Import ProjectItem from './ProjectItem.svelte'
- Import addProject from '$lib/db/operations'
- Import toast from 'svelte-5-french-toast'

- Create a local $state for newProjectTitle (string, initially empty)
- Header section: h2 "Projects" with item count badge (same pattern as ActionList header -- use `text-2xl font-bold` for heading, gray badge showing count)
- Stalled count indicator: If projectState.stalledCount > 0, show an amber text below the heading like "{N} stalled" with the same warning color scheme

- Create form: An inline form (not modal) below the header with:
  - Text input for project title, placeholder "New project outcome...", same styling as InboxCapture input
  - Submit button "Add Project" (blue, same style as action buttons)
  - On submit: call addProject(newProjectTitle.trim()), clear input, reload projectState.loadProjects(), show toast "Project created"
  - Disable submit if title is empty/whitespace

- Project list: Iterate over projectState.items, render ProjectItem for each
  - Pass isStalled={projectState.isStalled(project.id)}
  - Pass isExpanded={projectState.expandedId === project.id}
  - Pass onToggleExpand={() => projectState.expandItem(project.id)}
  - Pass onSave handler that calls projectState.loadProjects() to refresh list

- Empty state: When projectState.items is empty AND newProjectTitle is empty, show centered empty state with:
  - Folder/clipboard icon (SVG, gray)
  - "No active projects" in medium gray text
  - "Projects are multi-step outcomes. Create one to track your goals." in smaller gray text
  - Same styling pattern as inbox empty state

- Layout: Main container with `p-6` padding, max-width constraint `max-w-4xl mx-auto`

**src/routes/projects/+page.svelte:**
- Minimal route page (mirror /actions pattern exactly)
- Import onMount from 'svelte'
- Import ProjectList from '$lib/components/ProjectList.svelte'
- Import projectState from '$lib/stores/projects.svelte'
- onMount: call await projectState.loadProjects()
- Render: just `<ProjectList />`
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors. Verify the /projects route exists. Run `npm run build` to confirm the route compiles.
  </verify>
  <done>
/projects route loads and displays ProjectList. Users can create projects via inline form. List shows all active projects with stalled badges. Empty state displays when no projects exist. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. /projects route exists and renders ProjectList
4. ProjectItem shows stalled badge when isStalled is true
5. ProjectDetailPanel has save, complete, someday/maybe, and delete buttons
6. Create form adds projects and refreshes list
7. Empty state shows when no projects exist
</verification>

<success_criteria>
- /projects route renders project list with stalled indicators
- Users can create new projects from the page
- Detail panel allows editing title/notes, completing, moving to someday, and deleting
- Stalled badge appears on projects with no active next actions
- All components follow established Phase 3 patterns (ActionList, ActionItem, ActionDetailPanel)
- Zero TypeScript errors, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects-management/04-02-SUMMARY.md`
</output>
