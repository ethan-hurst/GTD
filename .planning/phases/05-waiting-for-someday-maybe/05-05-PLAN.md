---
phase: 05-waiting-for-someday-maybe
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All phase 5 success criteria verified by human testing"
    - "Waiting-for and someday/maybe features work end-to-end"
    - "No regressions in existing inbox, actions, or projects features"
  artifacts: []
  key_links: []
---

<objective>
Human verification of complete Waiting For and Someday/Maybe features.

Purpose: Verify all phase 5 requirements work correctly in the running application before marking phase complete.
Output: Verification confirmation or issue list for gap closure.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run build verification</name>
  <files></files>
  <action>
  1. Run `npm run build` to verify the full build succeeds with no errors
  2. Run `npx tsc --noEmit` to verify TypeScript compilation
  3. Start the dev server: `npm run dev` (note the URL for user)
  4. Report the dev server URL and build status to the user
  </action>
  <verify>Build and TypeScript compilation both succeed with zero errors.</verify>
  <done>Dev server running, build passing, ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete Waiting For and Someday/Maybe features:
  - /waiting route with inline add, overdue styling, resolve with undo
  - /someday route with inline add, category filter, promote to project/action
  - Sidebar navigation links (no badges)
  - Keyboard shortcuts: 'w' (waiting), 's' (someday)
  - Processing flow: delegate step with optional follow-up date
  </what-built>
  <how-to-verify>
  **WAIT-01: Waiting-for list with delegation tracking**
  1. Press 'w' or click "Waiting For" in sidebar -- verify /waiting loads
  2. Use inline form: enter title "Review draft", person "Alice", follow-up date (set to yesterday) -- click Add
  3. Verify item appears with "Delegated to: Alice" and overdue red styling (since date is past)
  4. Add another item with future follow-up date -- verify it does NOT have red styling
  5. Add a third item with no follow-up date -- verify it appears without date info

  **WAIT-02: Resolve waiting-for items**
  6. Click resolve on one of the items -- verify toast "Waiting-for item resolved" appears
  7. Click "Undo" on the toast -- verify item reappears in the list
  8. Resolve again without undo -- verify item disappears from list

  **SMBY-01: Someday/maybe list**
  9. Press 's' or click "Someday/Maybe" in sidebar -- verify /someday loads
  10. Use inline form: enter "Learn Spanish" -- click Add
  11. Verify item appears in list with no category badge
  12. Click item to expand -- assign "Learning" category
  13. Verify category badge appears on the item
  14. Add more items and verify category filter sidebar works (click category to filter)

  **SMBY-02: Promote someday/maybe to active**
  15. On a someday item, click "Project" promote button -- verify toast "Promoted to Projects"
  16. Navigate to /projects -- verify the promoted item appears as a project
  17. Go back to /someday, add another item, promote to "Action" -- verify it appears in /actions

  **Processing Integration**
  18. Go to Inbox, add a test item
  19. Process it: Actionable -> No -> Delegate -> enter person name + follow-up date -> Delegate
  20. Navigate to /waiting -- verify the delegated item appears with correct person and date
  21. Add another inbox item, process: Not actionable -> Someday/Maybe
  22. Navigate to /someday -- verify item appears

  **Regression Check**
  23. Navigate to Inbox, Next Actions, Projects -- verify they all still work
  24. Test keyboard shortcuts: '/' (inbox focus), 'n' (actions), 'p' (projects), 'w' (waiting), 's' (someday)

  **No-Badge Check**
  25. Verify Waiting For and Someday/Maybe sidebar links have NO count badges
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All phase 5 success criteria verified by human testing:
1. User can add items to waiting-for list with who delegated to and when
2. User can mark waiting-for items as resolved when they're complete
3. User can add ideas to someday/maybe list to park them
4. User can promote a someday/maybe item to an active project
5. Waiting-for and someday/maybe items don't appear in next actions lists
</verification>

<success_criteria>
- Human approves all 25 verification steps
- No regressions in existing features
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-waiting-for-someday-maybe/05-05-SUMMARY.md`
</output>
