---
phase: 08.1-ui-ux-review
plan: 06
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - src/routes/calendar/+page.svelte
  - src/lib/components/EventCalendar.svelte
  - src/lib/components/EventForm.svelte
  - src/lib/components/CalendarSidePanel.svelte
  - src/lib/components/IcsImport.svelte
  - src/routes/review/+page.svelte
  - src/lib/components/WeeklyReviewWizard.svelte
autonomous: true

must_haves:
  truths:
    - "Calendar toolbar buttons have consistent sizing and focus rings"
    - "View switcher (Day/Week/Month) has refined segmented control styling"
    - "Event form has polished inputs with shadow-sm and focus rings"
    - "Weekly Review start page has professional hero layout with refined CTA"
    - "Review wizard step sidebar has focus rings and refined active state"
    - "Review wizard navigation footer has consistent button patterns"
  artifacts:
    - path: "src/routes/calendar/+page.svelte"
      provides: "Polished calendar page"
      contains: "focus:ring"
    - path: "src/lib/components/EventForm.svelte"
      provides: "Polished event form"
      contains: "shadow"
    - path: "src/routes/review/+page.svelte"
      provides: "Polished review start page"
      contains: "focus:ring"
    - path: "src/lib/components/WeeklyReviewWizard.svelte"
      provides: "Polished review wizard"
      contains: "focus:ring"
  key_links:
    - from: "src/routes/calendar/+page.svelte"
      to: "src/lib/components/EventForm.svelte"
      via: "Calendar renders EventForm"
      pattern: "EventForm"
    - from: "src/routes/review/+page.svelte"
      to: "src/lib/components/WeeklyReviewWizard.svelte"
      via: "Review renders wizard"
      pattern: "WeeklyReviewWizard"
---

<objective>
Polish the Calendar and Weekly Review views -- the two most visually complex pages with toolbars, side panels, multi-step wizards, and rich interactions.

Purpose: Calendar is the most complex view with toolbar, view switcher, side panel, and event forms. Weekly Review is the ceremonial capstone of GTD workflow. Both need to feel premium to match the polish applied to simpler views.

Output: Polished calendar toolbar with refined segmented controls, polished event form, polished review wizard with refined step sidebar and hero start page.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-ui-ux-review/08.1-RESEARCH.md
@.planning/phases/08.1-ui-ux-review/08.1-01-SUMMARY.md

@src/routes/calendar/+page.svelte
@src/lib/components/EventCalendar.svelte
@src/lib/components/EventForm.svelte
@src/lib/components/CalendarSidePanel.svelte
@src/lib/components/IcsImport.svelte
@src/routes/review/+page.svelte
@src/lib/components/WeeklyReviewWizard.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Calendar toolbar, view switcher, and panels</name>
  <files>src/routes/calendar/+page.svelte, src/lib/components/EventForm.svelte, src/lib/components/CalendarSidePanel.svelte, src/lib/components/IcsImport.svelte, src/lib/components/EventCalendar.svelte</files>
  <action>
Read EventForm.svelte, CalendarSidePanel.svelte, IcsImport.svelte, and EventCalendar.svelte first.

Polish calendar/+page.svelte toolbar:

1. **Toolbar container:**
   - Change `px-6 py-4 border-b border-gray-200 dark:border-gray-800` to `px-6 py-3 border-b border-gray-100 dark:border-gray-800/50 bg-white dark:bg-gray-950` (subtler border, explicit background, slightly tighter padding)

2. **"New Event" button (primary, small):**
   - Add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-2`
   - Add `active:scale-[0.97]` for press feedback
   - Change `rounded` to `rounded-md` (consistent with other small buttons)
   - Add `shadow-sm hover:shadow-md` for depth

3. **Navigation buttons (prev/next arrows):**
   - Add `focus:outline-none focus:ring-2 focus:ring-gray-400/40 rounded-lg` (focus ring + softer corners)
   - Add `transition-all duration-150`
   - Ensure minimum 44x44px touch target (current p-2 with w-5 icon = ~36px -- increase to `p-2.5`)

4. **"Today" button:**
   - Add `focus:outline-none focus:ring-2 focus:ring-gray-400/40 focus:ring-offset-2`
   - Add `transition-all duration-150`

5. **Date label (h1):**
   - Keep `text-lg font-semibold` -- correct for the context
   - Add `tabular-nums` for stable date rendering

6. **Import button:**
   - Add `focus:outline-none focus:ring-2 focus:ring-gray-400/40 focus:ring-offset-2`
   - Add `transition-all duration-150`

7. **Actions toggle button:**
   - Add `focus:outline-none focus:ring-2 focus:ring-gray-400/40 focus:ring-offset-2`
   - Add `transition-all duration-150`

8. **View switcher (Day/Week/Month segmented control):**
   - Outer container: change `bg-gray-100 dark:bg-gray-800 rounded-lg p-1` to `bg-gray-100/80 dark:bg-gray-800/60 rounded-lg p-0.5` (subtler background, tighter padding for more refined segmented feel)
   - Active segment: change `bg-white dark:bg-gray-700 text-gray-900 dark:text-white shadow-sm` to `bg-white dark:bg-gray-700 text-gray-900 dark:text-white shadow-sm ring-1 ring-gray-200/50 dark:ring-gray-600/50` (add subtle ring for crisper active state)
   - Add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-1` to each segment button
   - Change `rounded` to `rounded-md` for each segment

9. **Vertical divider:**
   - Change `w-px h-6 bg-gray-300 dark:bg-gray-700` to `w-px h-5 bg-gray-200 dark:bg-gray-700/50` (subtler)

Polish EventForm.svelte:
- Panel: add `shadow-xl border-l border-gray-200 dark:border-gray-800` for side panel elevation
- All inputs: add `shadow-sm` for subtle depth
- All buttons: add focus rings
- Save button: add `active:scale-[0.98]` press feedback
- Delete button: add `focus:ring-red-500/40` ring
- Close button: add `focus:outline-none focus:ring-2 focus:ring-gray-400/40`

Polish CalendarSidePanel.svelte:
- Panel: add `border-l border-gray-200/70 dark:border-gray-800/50` (soft separation)
- Ensure action items within panel have hover states
- Add focus rings to any interactive elements

Polish IcsImport.svelte:
- Modal overlay: ensure `backdrop-blur-sm` for modern feel
- Modal: add `shadow-2xl rounded-xl` for premium elevation
- All buttons and inputs: add focus rings and shadow-sm

Polish EventCalendar.svelte (only styling-related changes):
- If there are wrapper classes, ensure they don't conflict with calendar library styles
- Only add focus:outline-none to any wrapper interactive elements if present
  </action>
  <verify>
Run `npm run dev`, navigate to Calendar:
1. Toolbar buttons should all have focus rings (Tab through them)
2. View switcher active state should have subtle ring + shadow
3. Click "New Event" -- form panel should have shadow-xl elevation
4. Nav arrows should be at least 44px touch targets
5. Build: `npx vite build` -- no errors.
  </verify>
  <done>Calendar toolbar has focus rings on all buttons, refined view switcher with ring+shadow active, elevated event form panel with polished inputs, proper touch targets, and refined CalendarSidePanel/IcsImport.</done>
</task>

<task type="auto">
  <name>Task 2: Polish Weekly Review start page and wizard</name>
  <files>src/routes/review/+page.svelte, src/lib/components/WeeklyReviewWizard.svelte</files>
  <action>
Polish review/+page.svelte (start page):

1. **Hero layout:**
   - Keep `flex items-center justify-center min-h-full` -- correct centering
   - The `text-center max-w-2xl px-8` wrapper: change to `text-center max-w-xl` (slightly tighter for more focused feel, remove px-8 since parent provides padding)

2. **Hero heading:**
   - Change `text-4xl font-bold` to `text-3xl font-bold` (slightly smaller -- text-4xl is very large for a start page)
   - Add `tracking-tight` for refined heading typography

3. **Subtitle:**
   - Keep `text-lg text-gray-500 dark:text-gray-400 mb-8` -- correct

4. **Last Review indicator:**
   - Wrap in a card: `bg-gray-50 dark:bg-gray-800/30 rounded-lg px-6 py-4 mb-8 inline-block` (give it visual containment)
   - "Last Review" label: keep `text-sm font-medium`
   - Overdue state: keep the warning styling -- it's effective

5. **Start button (hero CTA):**
   - Keep `px-8 py-4 text-base font-medium rounded-lg` (large button size -- correct for hero CTA)
   - Add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-2`
   - Add `active:scale-[0.98]`
   - Change `shadow-lg hover:shadow-xl` to `shadow-md hover:shadow-lg` (slightly more refined -- shadow-lg default is too heavy for a button)
   - Add `transition-all duration-200`

Polish WeeklyReviewWizard.svelte:

1. **Step sidebar:**
   - Progress bar: keep existing styling -- it's well done
   - Progress text: add `tabular-nums` for stable percentage rendering
   - Step buttons: add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-inset` (inset since buttons are full-width)
   - Active step: keep `bg-blue-50 dark:bg-blue-900/20 border-l-4 border-l-blue-600` -- this is good
   - Add `transition-all duration-150` to step buttons
   - Step indicators (numbered circles): already good styling -- keep
   - Completed steps: the green circle + checkmark is well done -- keep
   - Completed text strikethrough: change `line-through text-gray-500` to `line-through text-gray-400 dark:text-gray-500` (slightly lighter for completed items)

2. **Step content area:**
   - Keep `max-w-2xl mx-auto p-8` for content area -- correct (wizard content area can have more breathing room than list views)
   - Step title: keep `text-2xl font-bold` -- correct
   - Step description: keep `text-base text-gray-600 dark:text-gray-300` -- correct

3. **Context info box:**
   - Change from `bg-gray-50 dark:bg-gray-800/50 border border-gray-200 dark:border-gray-700 rounded-lg p-4` to `bg-gray-50/80 dark:bg-gray-800/30 border border-gray-200/80 dark:border-gray-700/50 rounded-lg p-4` (softer background and border)
   - Empty step (green text): keep `text-green-600 dark:text-green-400 font-medium` -- correct

4. **"Mark Step Complete" button:**
   - Add `focus:outline-none focus:ring-2 focus:ring-green-500/40 focus:ring-offset-2`
   - Add `active:scale-[0.98]`
   - Add `transition-all duration-150`
   - Add `shadow-sm hover:shadow-md` for depth

5. **Navigation footer:**
   - Footer container: change to `border-t border-gray-200/70 dark:border-gray-700/50 p-4 flex items-center justify-between bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm` (subtle frosted footer)
   - Back button: add `focus:outline-none focus:ring-2 focus:ring-gray-400/40 focus:ring-offset-2` and `transition-all duration-150`
   - Next button: add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-2` and `transition-all duration-150`
   - "Finish Review" button: add `focus:outline-none focus:ring-2 focus:ring-green-500/40 focus:ring-offset-2`, `active:scale-[0.98]`, `shadow-sm hover:shadow-md`, `transition-all duration-150`

6. **Sidebar border:**
   - Change `border-r border-gray-200 dark:border-gray-700` to `border-r border-gray-200/70 dark:border-gray-700/50` (softer)
  </action>
  <verify>
Run `npm run dev`, navigate to Weekly Review:
1. Start page heading should be text-3xl with tracking-tight
2. Last review indicator should be in a subtle card
3. Start button should have focus ring and shadow-md
4. Click Start -- wizard should show with polished step sidebar
5. Tab through steps -- should show inset focus rings
6. Navigation footer should have frosted glass effect
7. Mark a step complete, click Finish -- button should have green focus ring
8. Build: `npx vite build` -- no errors.
  </verify>
  <done>Review start page has refined hero layout with tracking-tight heading, contained last-review indicator, and polished CTA. Wizard has focus rings on step buttons, frosted navigation footer, polished Mark Complete and Finish buttons.</done>
</task>

</tasks>

<verification>
1. `npx vite build` passes without errors
2. Calendar toolbar buttons all have focus rings
3. View switcher has refined segmented control with ring+shadow
4. Event form panel has shadow-xl elevation
5. Review start page has text-3xl heading with tracking-tight
6. Wizard step buttons have inset focus rings
7. Navigation footer has frosted glass effect
</verification>

<success_criteria>
- Calendar toolbar feels polished: consistent button sizes, focus rings, refined segmented control
- Event form feels elevated with shadow-xl panel
- Review start page feels like a premium app hero section
- Wizard step sidebar has clear keyboard navigation
- Navigation footer feels modern with backdrop-blur
- All interactive elements have keyboard-accessible focus rings
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-ui-ux-review/08.1-06-SUMMARY.md`
</output>
