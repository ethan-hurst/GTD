---
phase: 08.1-ui-ux-review
plan: 05
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - src/lib/components/WaitingForList.svelte
  - src/lib/components/WaitingForItem.svelte
  - src/lib/components/SomedayMaybeList.svelte
  - src/lib/components/SomedayMaybeItem.svelte
  - src/routes/settings/+page.svelte
autonomous: true

must_haves:
  truths:
    - "Waiting For list form inputs have shadow-sm and focus rings"
    - "Overdue waiting items have clear visual urgency without being alarming"
    - "Someday/Maybe category sidebar buttons have focus rings matching main sidebar style"
    - "Settings cards have refined shadows and consistent button patterns"
    - "All interactive elements across all three views have keyboard-accessible focus rings"
  artifacts:
    - path: "src/lib/components/WaitingForList.svelte"
      provides: "Polished waiting-for list"
      contains: "focus:ring"
    - path: "src/lib/components/SomedayMaybeList.svelte"
      provides: "Polished someday/maybe list"
      contains: "focus:ring"
    - path: "src/routes/settings/+page.svelte"
      provides: "Polished settings page"
      contains: "shadow"
  key_links:
    - from: "src/lib/components/WaitingForList.svelte"
      to: "src/lib/components/WaitingForItem.svelte"
      via: "List renders items"
      pattern: "WaitingForItem"
    - from: "src/lib/components/SomedayMaybeList.svelte"
      to: "src/lib/components/SomedayMaybeItem.svelte"
      via: "List renders items"
      pattern: "SomedayMaybeItem"
---

<objective>
Polish the Waiting For, Someday/Maybe, and Settings views -- the three secondary views that complete the GTD system.

Purpose: Even secondary views must feel professionally polished. Inconsistency in these views undermines the polish applied to primary views. Settings in particular is where users assess app quality -- a polished settings page signals a well-crafted app.

Output: Polished WaitingForList/Item with overdue indicators, polished SomedayMaybeList/Item with refined category sidebar, polished Settings with elevated card styling.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-ui-ux-review/08.1-RESEARCH.md
@.planning/phases/08.1-ui-ux-review/08.1-01-SUMMARY.md

@src/lib/components/WaitingForList.svelte
@src/lib/components/WaitingForItem.svelte
@src/lib/components/SomedayMaybeList.svelte
@src/lib/components/SomedayMaybeItem.svelte
@src/routes/settings/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish WaitingForList/Item and SomedayMaybeList/Item</name>
  <files>src/lib/components/WaitingForList.svelte, src/lib/components/WaitingForItem.svelte, src/lib/components/SomedayMaybeList.svelte, src/lib/components/SomedayMaybeItem.svelte</files>
  <action>
Read WaitingForItem.svelte and SomedayMaybeItem.svelte first to understand current structure.

Polish WaitingForList.svelte:

1. **Layout:** Keep `p-6 max-w-4xl mx-auto` (already consistent)

2. **Header count badge:**
   - Add `tabular-nums` to the count badge span

3. **Overdue indicator:**
   - Change `text-sm text-red-600 dark:text-red-400` to `text-sm text-red-600 dark:text-red-400 font-medium` (match ProjectList stalled pattern)

4. **Add form:**
   - Both text inputs: add `shadow-sm` for subtle depth
   - Date input: add `shadow-sm`
   - "Follow up by" label: keep current styling
   - "Add" button: add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-2` and `transition-all duration-150`
   - All inputs: ensure consistent `py-2` height (they currently have py-2 -- good)

5. **Empty state:** Already good (uses consistent pattern) -- no changes needed

Polish WaitingForItem.svelte:
- Add focus ring to expand/collapse trigger
- Add `transition-all duration-150` to hover states
- Overdue items: add `border-l-2 border-red-400` left accent (subtle urgency indicator)
- Ensure person name badge has `font-medium`
- Add `group` class to row container for group-hover effects

Polish SomedayMaybeList.svelte:

1. **Category sidebar:**
   - Change `w-48` sidebar border from `border-r border-gray-200 dark:border-gray-800` to `border-r border-gray-200/70 dark:border-gray-800/50` (softer, matching main sidebar)
   - Category buttons: add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-1` for keyboard navigation
   - Active state: change from `bg-gray-200 dark:bg-gray-800` to `bg-white dark:bg-gray-800 shadow-sm font-semibold` (card-like elevation matching main sidebar active state)
   - Add `transition-all duration-150` to all category buttons

2. **Main content area:** Keep `flex-1 overflow-auto p-6` -- correct

3. **Count badge in header:**
   - Add `tabular-nums` to the count badge

4. **Add form:**
   - Input: add `shadow-sm`
   - Button: add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-2` and `transition-all duration-150`

5. **Empty state:** Already good -- no changes needed

Polish SomedayMaybeItem.svelte:
- Add focus ring to expand/collapse trigger
- Add `transition-all duration-150` to hover states
- Category badge (if present): add `transition-colors duration-100`
- Promote buttons: add focus rings and `transition-all duration-150`
  </action>
  <verify>
Run `npm run dev`:
1. Navigate to Waiting For -- form inputs should have shadow-sm, Add button has focus ring
2. Tab through waiting items -- should show focus rings
3. Navigate to Someday/Maybe -- category sidebar buttons should have focus rings
4. Active category should have shadow-sm elevation (card-like)
5. Build: `npx vite build` -- no errors.
  </verify>
  <done>WaitingForList/Item have shadow-sm inputs, focus rings, overdue left-accent indicators. SomedayMaybeList has polished category sidebar with elevated active state. Both item types have focus rings and smooth transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Polish Settings page with elevated cards</name>
  <files>src/routes/settings/+page.svelte</files>
  <action>
Polish the Settings page:

1. **Page header:**
   - Change `text-3xl font-bold` to `text-2xl font-bold` to match other page headings (consistency -- all pages use text-2xl)
   - Change `mb-8` to `mb-6` for consistency

2. **Layout wrapper:**
   - Change `max-w-2xl mx-auto p-8` to `max-w-2xl mx-auto p-6` (consistent p-6 across all pages)

3. **Setting cards (Export, Import, Onboarding sections):**
   - Upgrade from `bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 rounded-lg p-6 mb-6` to:
     `bg-white dark:bg-gray-900 border border-gray-200/80 dark:border-gray-800/60 rounded-xl p-6 mb-4 shadow-sm hover:shadow-md transition-shadow duration-200`
   - rounded-xl (softer corners for cards)
   - shadow-sm + hover:shadow-md (subtle depth + hover elevation)
   - mb-4 instead of mb-6 (tighter card grouping)
   - Change last card mb-6 to mb-0 (no bottom margin on last card)

4. **Card headings:**
   - Keep `text-lg font-semibold` -- correct

5. **Buttons (Export Data, Import Data, Reset Onboarding):**
   - Current: `bg-gray-900 dark:bg-gray-100 text-white dark:text-gray-900` (inverted theme)
   - This is actually a nice pattern -- keep it for settings (differentiates from blue primary elsewhere)
   - Add focus ring: `focus:outline-none focus:ring-2 focus:ring-gray-500/40 focus:ring-offset-2`
   - Add `transition-all duration-150`
   - Add `active:scale-[0.98]` for press feedback
   - Ensure `rounded-md` is consistent (currently is)

6. **Warning banner (Import section):**
   - Keep existing amber styling -- it's well done
   - Add `shadow-sm` inside the warning box for subtle depth

7. **Status message:**
   - Keep existing styling (green/red conditional) -- it's correct
   - Add `transition-all duration-200` for smooth appear
   - Consider adding `shadow-sm` to status message for subtle depth

8. **Disabled state:**
   - Keep `disabled:opacity-50 disabled:cursor-not-allowed` -- correct
  </action>
  <verify>
Run `npm run dev`, navigate to Settings:
1. Page heading should be text-2xl (matching other pages)
2. Cards should have shadow-sm with hover:shadow-md elevation
3. Cards should have rounded-xl (softer corners)
4. Buttons should have focus rings and press feedback
5. Build: `npx vite build` -- no errors.
  </verify>
  <done>Settings page has consistent text-2xl heading, p-6 padding, elevated shadow cards with rounded-xl, focus-ringed buttons with press feedback, and refined spacing.</done>
</task>

</tasks>

<verification>
1. `npx vite build` passes without errors
2. WaitingFor form inputs have shadow-sm, buttons have focus rings
3. SomedayMaybe category sidebar has elevated active state
4. Settings cards have shadow-sm + hover:shadow-md with rounded-xl
5. Settings heading matches other pages (text-2xl)
6. All interactive elements have keyboard-accessible focus rings
</verification>

<success_criteria>
- Waiting For list form is polished with shadow inputs and focus-ringed buttons
- Overdue items have subtle left-border accent for urgency
- Someday/Maybe category sidebar matches main sidebar polish (elevated active, focus rings)
- Settings page feels like a premium app: elevated cards, soft corners, hover effects
- All pages use consistent p-6 padding and text-2xl headings
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-ui-ux-review/08.1-05-SUMMARY.md`
</output>
