---
phase: 08.1-ui-ux-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/components/Sidebar.svelte
  - src/lib/components/SearchBar.svelte
  - src/lib/components/ThemeToggle.svelte
  - src/lib/components/ContextList.svelte
  - src/lib/components/FeatureHint.svelte
autonomous: true

must_haves:
  truths:
    - "Sidebar navigation links have visible focus rings for keyboard navigation"
    - "Active sidebar link has a refined left accent indicator and slightly elevated visual weight"
    - "Sidebar has professional visual hierarchy: logo, nav sections with subtle separators, footer"
    - "Search bar has polished focus state with smooth expansion feel"
    - "Theme toggle looks intentional and polished, not like an afterthought"
  artifacts:
    - path: "src/lib/components/Sidebar.svelte"
      provides: "Polished navigation sidebar"
      contains: "focus:ring"
    - path: "src/lib/components/SearchBar.svelte"
      provides: "Refined search input"
      contains: "transition"
    - path: "src/lib/components/ThemeToggle.svelte"
      provides: "Polished theme switcher"
  key_links:
    - from: "src/lib/components/Sidebar.svelte"
      to: "src/lib/components/ContextList.svelte"
      via: "Sidebar renders ContextList"
      pattern: "ContextList"
---

<objective>
Polish the Sidebar navigation, SearchBar, ThemeToggle, and ContextList -- the persistent UI elements visible on every page. These components form the user's constant frame of reference and must feel refined.

Purpose: The sidebar is the most-seen UI element. If it looks amateur, the whole app feels amateur. Polish here has outsized impact on perceived quality. SearchBar and ThemeToggle are in the persistent header/footer and need equal refinement.

Output: Professionally polished sidebar with refined nav links, focus states, subtle animations. Polished SearchBar and ThemeToggle.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-ui-ux-review/08.1-RESEARCH.md

@src/lib/components/Sidebar.svelte
@src/lib/components/SearchBar.svelte
@src/lib/components/ThemeToggle.svelte
@src/lib/components/ContextList.svelte
@src/lib/components/FeatureHint.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish Sidebar navigation with refined interactions</name>
  <files>src/lib/components/Sidebar.svelte, src/lib/components/ContextList.svelte</files>
  <action>
Polish Sidebar.svelte for professional-quality navigation:

1. **Sidebar container:**
   - Change background from `bg-gray-50 dark:bg-gray-900` to `bg-gray-50/80 dark:bg-gray-900/95` with `backdrop-blur-sm` for a modern frosted-glass subtle effect
   - Change border from `border-r border-gray-200 dark:border-gray-800` to `border-r border-gray-200/70 dark:border-gray-800/50` (softer separation)

2. **Logo area:**
   - Keep the existing GTD expand animation (it's delightful)
   - Reduce `p-6` to `px-5 py-4` for tighter, more app-like spacing
   - Change border from `border-b border-gray-200 dark:border-gray-800` to `border-b border-gray-200/50 dark:border-gray-800/50` (lighter)

3. **Navigation links -- all `<a>` tags:**
   - Add focus ring: `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-1` to EVERY nav link
   - **Active state upgrade:** Change from `bg-gray-200 dark:bg-gray-800` to `bg-white dark:bg-gray-800 shadow-sm text-gray-900 dark:text-gray-100 font-semibold` -- active items should "pop" with a card-like elevation instead of flat background
   - **Hover state refinement:** Change `hover:bg-gray-100 dark:hover:bg-gray-800` to `hover:bg-gray-100/70 dark:hover:bg-gray-800/70` (softer hover)
   - Add `transition-all duration-150` (upgrade from `transition-colors` for shadow transitions too)

4. **Badge refinements (inbox count, stalled count, overdue):**
   - Inbox badge: Keep `bg-blue-500 text-white` but add `shadow-sm` for depth
   - Stalled badge: Keep `bg-yellow-500 text-white` but add `shadow-sm`
   - Overdue badge: Keep `bg-red-500 text-white` but add `shadow-sm` and `animate-pulse` (subtle attention-drawing for overdue)

5. **Section separators:**
   - Change from `border-t border-gray-200 dark:border-gray-700 my-2` to `border-t border-gray-200/50 dark:border-gray-700/30 my-2` (much lighter -- nearly invisible but still creates visual grouping)

6. **Footer (theme toggle area):**
   - Change `border-t border-gray-200 dark:border-gray-800` to `border-t border-gray-200/50 dark:border-gray-800/50`
   - Keep `p-4` padding

Also polish ContextList.svelte:
- Read it first to understand the current structure
- Add focus rings to context filter buttons
- Ensure hover states match the sidebar nav link style
- Add `transition-all duration-150` to context buttons
  </action>
  <verify>
Run `npm run dev` and test:
1. Tab through sidebar links -- all should show visible blue focus rings
2. Active link should have card-like elevation (shadow-sm) instead of flat gray
3. Hover states should be subtle and smooth
4. Overdue badge should pulse gently
5. Section separators should be very subtle
Build with `npx vite build` -- no errors.
  </verify>
  <done>Sidebar nav links have focus rings, refined active states with shadow elevation, smooth hover transitions, and subtle separators. Badges have shadows and overdue pulses. ContextList buttons match sidebar polish.</done>
</task>

<task type="auto">
  <name>Task 2: Polish SearchBar and ThemeToggle</name>
  <files>src/lib/components/SearchBar.svelte, src/lib/components/ThemeToggle.svelte, src/lib/components/FeatureHint.svelte</files>
  <action>
Polish SearchBar.svelte:

1. **Search input refinement:**
   - Change background from `bg-gray-100 dark:bg-gray-800` to `bg-gray-100/70 dark:bg-gray-800/60` (subtler)
   - Add `border border-transparent` base state
   - On focus, add `focus:border-gray-200 dark:focus:border-gray-700 focus:shadow-sm` in addition to existing ring
   - Change `py-1.5` to `py-2` for a slightly larger, more comfortable click target (44px minimum touch target)
   - Add `transition-all duration-200` for smooth focus expansion feel

2. **Results dropdown refinement:**
   - Add `shadow-elevated` class (or `shadow-xl`) to the dropdown for more professional depth
   - Change `rounded-md` to `rounded-lg` on the dropdown for softer appearance
   - Add `border border-gray-200/80 dark:border-gray-700/80` (slightly transparent border)
   - Result items: Change `hover:bg-gray-50 dark:hover:bg-gray-700` to `hover:bg-blue-50/50 dark:hover:bg-blue-900/20` (blue tint on hover signals clickability)
   - Selected item: Change `bg-gray-100 dark:bg-gray-700` to `bg-blue-50 dark:bg-blue-900/30` with `text-blue-900 dark:text-blue-100`
   - Add `transition-colors duration-100` to result items

3. **Search icon:**
   - Keep the current gray-400 color -- it's correct for a muted search icon

Polish ThemeToggle.svelte:
- Read the current component first
- Add focus ring to the toggle button/control
- Ensure smooth transition between light/dark icons with `transition-all duration-200`
- If it's a button, add `rounded-lg p-2 hover:bg-gray-100 dark:hover:bg-gray-800` for clear affordance
- Add `cursor-pointer` if not already present

Polish FeatureHint.svelte:
- Read the current component first
- Ensure tooltip/hint popover has `shadow-lg rounded-lg` for professional appearance
- Ensure smooth enter/exit transitions
- Keep existing functionality, just refine the visual treatment
  </action>
  <verify>
Run `npm run dev` and test:
1. Click search bar -- should smoothly gain focus with subtle shadow
2. Type a query -- dropdown should appear with elevated shadow
3. Arrow through results -- selected item should have blue tint
4. Theme toggle should have visible focus ring and smooth transitions
5. Build with `npx vite build` -- no errors.
  </verify>
  <done>SearchBar has refined focus transition, elevated dropdown shadow, blue-tinted selection. ThemeToggle has focus ring and smooth transitions. FeatureHint has polished popover styling.</done>
</task>

</tasks>

<verification>
1. `npx vite build` passes without errors
2. All sidebar nav links have visible focus rings (Tab through them)
3. Active sidebar link uses shadow elevation instead of flat gray
4. SearchBar dropdown has elevated shadow and blue selection tint
5. ThemeToggle has focus ring and smooth icon transition
6. Overdue review badge pulses gently
</verification>

<success_criteria>
- Every interactive element in the sidebar has a visible focus ring
- Active navigation state clearly distinguishes current page with shadow elevation
- SearchBar feels polished with smooth focus/result transitions
- ThemeToggle looks intentional and has proper interaction states
- All transitions are smooth (150-200ms, ease-out)
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-ui-ux-review/08.1-02-SUMMARY.md`
</output>
