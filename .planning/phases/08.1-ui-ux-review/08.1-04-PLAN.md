---
phase: 08.1-ui-ux-review
plan: 04
type: execute
wave: 2
depends_on: ["08.1-01"]
files_modified:
  - src/lib/components/ActionList.svelte
  - src/lib/components/ActionItem.svelte
  - src/lib/components/ActionDetailPanel.svelte
  - src/lib/components/ProjectList.svelte
  - src/lib/components/ProjectItem.svelte
  - src/lib/components/ProjectDetailPanel.svelte
autonomous: true

must_haves:
  truths:
    - "Action items have refined hover states with subtle background transition"
    - "Complete button (circle) has visible hover and focus states"
    - "Bulk action buttons have focus rings and consistent sizing"
    - "Project items have refined expand/collapse with smooth transitions"
    - "Detail panels have elevated shadow and polished form elements"
    - "Create project form input and button have consistent styling with other forms"
  artifacts:
    - path: "src/lib/components/ActionItem.svelte"
      provides: "Polished action item rows"
      contains: "focus:ring"
    - path: "src/lib/components/ProjectList.svelte"
      provides: "Polished project list"
      contains: "focus:ring"
    - path: "src/lib/components/ActionDetailPanel.svelte"
      provides: "Polished action detail panel"
      contains: "shadow"
  key_links:
    - from: "src/lib/components/ActionList.svelte"
      to: "src/lib/components/ActionItem.svelte"
      via: "List renders items"
      pattern: "ActionItem"
    - from: "src/lib/components/ProjectList.svelte"
      to: "src/lib/components/ProjectItem.svelte"
      via: "List renders items"
      pattern: "ProjectItem"
---

<objective>
Polish the Next Actions and Projects views -- the two most complex list views with item interactions, detail panels, and CRUD forms.

Purpose: Actions and Projects are where users spend most of their management time. These views have the most interactive elements (checkboxes, expand/collapse, inline edit, drag-drop, bulk actions, detail panels) and need the most attention to feel professional.

Output: Polished ActionList/ActionItem/ActionDetailPanel with refined interactions. Polished ProjectList/ProjectItem/ProjectDetailPanel with consistent patterns.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-ui-ux-review/08.1-RESEARCH.md
@.planning/phases/08.1-ui-ux-review/08.1-01-SUMMARY.md

@src/lib/components/ActionList.svelte
@src/lib/components/ActionItem.svelte
@src/lib/components/ActionDetailPanel.svelte
@src/lib/components/ProjectList.svelte
@src/lib/components/ProjectItem.svelte
@src/lib/components/ProjectDetailPanel.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish ActionList and ActionItem interactions</name>
  <files>src/lib/components/ActionList.svelte, src/lib/components/ActionItem.svelte</files>
  <action>
Polish ActionList.svelte:

1. **Container layout:**
   - Current `flex-1 overflow-auto p-6` is correct -- keep it
   - Add `max-w-4xl` to match other list views' max width (currently unconstrained since it sits inside the flex layout, but adding max-w-4xl aligns with other views when sidebar is present)

2. **Header refinement:**
   - Keep `text-2xl font-bold` for h2 (consistent)
   - Add `tabular-nums` to the item count for stable number width

3. **Bulk action buttons:**
   - "Complete selected" green button: add `focus:outline-none focus:ring-2 focus:ring-green-500/40 focus:ring-offset-2` and `transition-all duration-150`
   - "Clear" button: add `focus:outline-none focus:ring-2 focus:ring-gray-400/40 focus:ring-offset-2` and `transition-all duration-150`
   - Both: standardize to `px-3 py-1.5` (small button size -- correct for toolbar actions)

4. **Empty state refinement:**
   - Add the "no actions" link: add `font-medium` to the blue link text for slightly stronger affordance

5. **Context group headers (All view):**
   - Change from `text-sm font-semibold text-gray-700 dark:text-gray-300 mb-2 px-2` to `text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-2 px-3` (smaller, all-caps section label -- more refined hierarchy)

Polish ActionItem.svelte:

1. **Row container:**
   - Keep `flex items-start gap-3 p-3 hover:bg-gray-50 dark:hover:bg-gray-800/50 transition-colors cursor-pointer rounded-md`
   - Add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-inset` for keyboard focus (it has tabindex="0")
   - Add `group` class for group-hover child interactions

2. **Completion circle button:**
   - Add `focus:outline-none focus:ring-2 focus:ring-blue-500/40` to the circle button
   - Add `transition-all duration-150` (upgrade from transition-colors for border animation)
   - Enhance hover: change `hover:border-blue-500` to `hover:border-blue-500 hover:bg-blue-50 dark:hover:bg-blue-900/20` (fill hint on hover)

3. **Inline title editing:**
   - Edit input: keep existing styling (it's good with focus:ring-2 ring-blue-500)
   - Title h3 hover: change `hover:bg-gray-100 dark:hover:bg-gray-700` to `hover:bg-gray-100/70 dark:hover:bg-gray-800/50` (softer hover)

4. **Badges:**
   - Context badge: add `transition-colors duration-100`
   - Project badge: keep existing purple styling, add `transition-colors duration-100`
   - Both badges: ensure they have `cursor-default` unless clickable

5. **Relative time:**
   - Keep `text-xs text-gray-400` -- correct for de-emphasized metadata

6. **Completing animation:**
   - Keep the existing fade transition -- it's well implemented
   - Change green circle border to `border-green-500 bg-green-500` (fill solidly on complete)
  </action>
  <verify>
Run `npm run dev`, navigate to Next Actions:
1. Tab to action items -- should show inset focus ring
2. Hover over completion circle -- should show blue fill hint
3. Bulk action buttons should have focus rings
4. Context group headers should be uppercase, smaller
5. Build: `npx vite build` -- no errors.
  </verify>
  <done>ActionList has max-width, tabular numbers, refined group headers, focus rings on bulk buttons. ActionItem has focus ring on row, blue-fill hint on circle hover, refined badges.</done>
</task>

<task type="auto">
  <name>Task 2: Polish ProjectList, ProjectItem, and both detail panels</name>
  <files>src/lib/components/ProjectList.svelte, src/lib/components/ProjectItem.svelte, src/lib/components/ProjectDetailPanel.svelte, src/lib/components/ActionDetailPanel.svelte</files>
  <action>
Read ProjectItem.svelte, ProjectDetailPanel.svelte, and ActionDetailPanel.svelte first to understand current structure.

Polish ProjectList.svelte:

1. **Layout consistency:**
   - Current `p-6 max-w-4xl mx-auto` is correct -- keep it

2. **Header count badge:**
   - Change inline badge from `ml-2 px-2 py-0.5 text-sm font-normal text-gray-600 dark:text-gray-400 bg-gray-100 dark:bg-gray-800 rounded` to same with `tabular-nums` added

3. **Create form:**
   - Input: add `shadow-sm` for subtle depth, keep existing focus ring
   - Button "Add Project": add `focus:outline-none focus:ring-2 focus:ring-blue-500/40 focus:ring-offset-2` and `transition-all duration-150`
   - Both input and button: ensure both have consistent `py-2` height

4. **Stalled indicator:**
   - Change `text-sm text-amber-600 dark:text-amber-400` to `text-sm text-amber-600 dark:text-amber-400 font-medium` (slightly stronger emphasis for a warning)

Polish ProjectItem.svelte:
- Add focus ring to the expand/collapse trigger (whatever the clickable element is)
- Add `transition-all duration-150` to hover/expand states
- Ensure stalled items have `border-l-2 border-amber-400` or similar left accent for visual urgency
- Add `group` class for parent-hover child effects

Polish ActionDetailPanel.svelte and ProjectDetailPanel.svelte:
Both panels should follow same polish pattern:

1. **Panel container:**
   - Add `shadow-lg` or `shadow-elevated` for panel elevation
   - Ensure `rounded-lg` for soft corners
   - Add `border border-gray-200 dark:border-gray-800` if not present
   - Add smooth enter animation if not present: use Svelte `slide` or `fly` transition

2. **Form elements inside panels:**
   - All inputs: add `shadow-sm` default, ensure `focus:ring-2 focus:ring-blue-500/40`
   - All buttons: standardize to `px-4 py-2` (medium size), add focus rings
   - Textareas: add `shadow-sm`, ensure `resize-none` if appropriate

3. **Panel headers:**
   - Ensure `font-semibold text-gray-900 dark:text-gray-100` for panel titles
   - Close button: ensure `rounded-lg p-1.5 hover:bg-gray-100 dark:hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-gray-400/40 transition-colors`

4. **Save/Cancel/Delete buttons:**
   - Save: blue primary with focus ring
   - Cancel: gray secondary with focus ring
   - Delete: red with focus ring, add `hover:shadow-sm` for subtle danger emphasis
  </action>
  <verify>
Run `npm run dev`, navigate to Projects:
1. Create form input has shadow-sm, button has focus ring
2. Click a project to expand -- detail panel should slide in with shadow-lg
3. All panel form elements should have shadow-sm and focus rings
4. Navigate to Actions, expand an action -- same polish on ActionDetailPanel
5. Build: `npx vite build` -- no errors.
  </verify>
  <done>ProjectList/Item have consistent polish with focus rings, stalled indicators, refined forms. ActionDetailPanel and ProjectDetailPanel have elevated shadows, polished form elements, and smooth transitions.</done>
</task>

</tasks>

<verification>
1. `npx vite build` passes without errors
2. ActionItem rows have keyboard focus rings (Tab test)
3. Completion circles show blue fill hint on hover
4. Context group headers are uppercase/smaller
5. ProjectList create form has shadow-sm input and focus-ringed button
6. Detail panels have shadow-lg elevation and polished forms
</verification>

<success_criteria>
- Action items feel interactive: focus rings, hover hints, smooth transitions
- Project items have clear stalled indicators and polished expand/collapse
- Detail panels feel elevated and professional with shadow-lg
- All form elements across both views have consistent shadow-sm + focus ring pattern
- Bulk action toolbar has properly styled buttons
- Build compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-ui-ux-review/08.1-04-SUMMARY.md`
</output>
