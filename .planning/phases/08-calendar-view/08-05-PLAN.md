---
phase: 08-calendar-view
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/lib/components/IcsImport.svelte
  - src/lib/components/CalendarSidePanel.svelte
  - src/routes/calendar/+page.svelte
  - src/lib/components/EventCalendar.svelte
autonomous: true

must_haves:
  truths:
    - "User can upload a .ics file and events appear on the calendar"
    - "User sees import progress and count of imported events"
    - "Side panel shows today's next actions alongside the calendar"
    - "Recurring events from ICS import expand correctly on the calendar"
  artifacts:
    - path: "src/lib/components/IcsImport.svelte"
      provides: "ICS file upload and import UI"
      contains: "IcsImport"
    - path: "src/lib/components/CalendarSidePanel.svelte"
      provides: "Next actions panel for calendar context"
      contains: "CalendarSidePanel"
  key_links:
    - from: "src/lib/components/IcsImport.svelte"
      to: "src/lib/utils/ics-parser.ts"
      via: "parseICS import"
      pattern: "import.*parseICS"
    - from: "src/lib/components/IcsImport.svelte"
      to: "src/lib/stores/calendar.svelte.ts"
      via: "calendarState.bulkImport"
      pattern: "calendarState\\.bulkImport"
    - from: "src/lib/components/CalendarSidePanel.svelte"
      to: "src/lib/stores/actions.svelte.ts"
      via: "actionState for next actions"
      pattern: "import.*actionState"
    - from: "src/routes/calendar/+page.svelte"
      to: "src/lib/utils/recurrence.ts"
      via: "expandAllRecurrences for display"
      pattern: "import.*expandAllRecurrences"
---

<objective>
Add ICS file import capability and a side panel showing next actions alongside the calendar.

Purpose: Users need to import events from external calendars (Outlook, Google Calendar exports) via .ics file upload. The side panel shows GTD next actions alongside the calendar, enabling the core GTD question: "What can I do now given my schedule?" This is where calendar meets GTD methodology. Additionally, wire recurrence expansion so recurring events display correctly.
Output: IcsImport.svelte for file upload, CalendarSidePanel.svelte for next actions, recurrence expansion wired into calendar display.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-calendar-view/08-CONTEXT.md
@src/lib/utils/ics-parser.ts
@src/lib/utils/recurrence.ts
@src/lib/stores/calendar.svelte.ts
@src/lib/stores/actions.svelte.ts
@src/routes/calendar/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ICS import component and wire recurrence expansion</name>
  <files>src/lib/components/IcsImport.svelte, src/routes/calendar/+page.svelte, src/lib/components/EventCalendar.svelte</files>
  <action>
1. Create `src/lib/components/IcsImport.svelte`:

   A modal/dialog component for importing .ics files. Props:
   ```typescript
   interface IcsImportProps {
     onClose: () => void;
     onImported: () => void;
   }
   ```

   UI:
   - Modal overlay with white card (use same modal pattern as other app dialogs or a centered card on overlay)
   - Title: "Import Calendar Events"
   - File input: `<input type="file" accept=".ics,.ical,.ifb,.icalendar">` styled as a drop zone with dashed border
   - Support drag-and-drop onto the zone (use native HTML5 drag events: dragover, drop)
   - After file selected/dropped:
     a. Read file content via FileReader.readAsText()
     b. Validate with validateICS()
     c. If invalid, show error message "Invalid ICS file format"
     d. Parse with parseICS(content, filename)
     e. Show preview: "Found {N} events. Import all?"
     f. Import button: call calendarState.bulkImport(events)
     g. Show success: "Imported {N} events" toast, call onImported()
   - Cancel button: calls onClose
   - Loading state while parsing/importing

2. Wire recurrence expansion into the calendar display:

   In `src/routes/calendar/+page.svelte`:
   - Import `expandAllRecurrences` from `$lib/utils/recurrence`
   - Create a derived value that computes the visible date range based on currentView and currentDate:
     - Day view: start of day to end of day
     - Week view: start of week (Monday) to end of week (Sunday)
     - Month view: first day of month to last day of month (+ overflow days for grid)
   - Create a derived value `displayEvents` that calls `expandAllRecurrences(calendarState.events, rangeStart, rangeEnd)` to expand recurring events for the visible range
   - Pass `displayEvents` to EventCalendar instead of raw `calendarState.events`

3. In `src/lib/components/EventCalendar.svelte`:
   - Ensure the events prop accepts expanded events (same CalendarEvent type, just more instances)
   - No changes needed if already mapping CalendarEvent[] to library format

4. Add "Import .ics" button to the calendar page toolbar (next to "New Event" button):
   - Opens the IcsImport modal
   - After import completes, reload events and close modal

State management in calendar page:
```typescript
let showImport = $state(false);
```
  </action>
  <verify>Run `npm run dev`. Test: (1) click "Import .ics" button - modal opens, (2) drag or select a .ics file - events are parsed and preview shown, (3) click Import - events appear on calendar with toast confirmation, (4) if a recurring event was imported, verify it shows on multiple dates within the visible range.</verify>
  <done>IcsImport component supports file input and drag-and-drop. ICS files are parsed via ics-parser.ts. Preview shows event count before import. Bulk import adds events to database. Recurrence expansion is wired so recurring events display correctly across the visible date range. Import button in toolbar opens the modal.</done>
</task>

<task type="auto">
  <name>Task 2: Create CalendarSidePanel with next actions for GTD context</name>
  <files>src/lib/components/CalendarSidePanel.svelte, src/routes/calendar/+page.svelte</files>
  <action>
1. Create `src/lib/components/CalendarSidePanel.svelte`:

   A side panel component showing next actions alongside the calendar for GTD planning context.

   Props:
   ```typescript
   interface CalendarSidePanelProps {
     currentView: 'dayGridMonth' | 'timeGridWeek' | 'timeGridDay';
     currentDate: Date;
   }
   ```

   Content strategy (per RESEARCH.md open questions):
   - **Day view**: Show today's next actions (all active next actions, grouped by context)
   - **Week/Month view**: Show all active next actions (same, since filtering by date range doesn't apply to non-time-specific actions)

   Implementation:
   - Import `actionState` from actions store
   - On mount, call `actionState.loadActions()` if not already loaded
   - Display next actions grouped by context (reuse the grouping logic pattern from ActionList)
   - Each action item shows: checkbox (complete), title, context badge
   - Clicking an action navigates to /actions (or opens detail — simpler: just show title and context, clicking navigates)
   - Clicking the checkbox calls `completeAction(id)` with toast notification and undo (reuse existing pattern)

   Layout:
   - Width: 280px (narrower than main event form panel)
   - Header: "Next Actions" with count badge
   - Scrollable list below header
   - Empty state: "No next actions. Your plate is clear!" (green text, matching existing empty states)
   - Separator at top: border-l to visually divide from calendar

   Style: Match existing sidebar/panel patterns. Same font sizes, colors, Tailwind classes.

2. Wire CalendarSidePanel into `/calendar/+page.svelte`:

   Add a toggle button in the toolbar: panel icon or "Actions" text button that toggles the side panel visibility.

   ```typescript
   let showSidePanel = $state(true); // Default visible on desktop
   ```

   Layout when side panel is open:
   ```
   <div class="flex h-full">
     <div class="flex-1 flex flex-col">
       <toolbar />
       <EventCalendar />
     </div>
     {#if showForm}
       <EventForm panel />
     {/if}
     {#if showSidePanel && !showForm}
       <CalendarSidePanel />
     {/if}
   </div>
   ```

   When EventForm is open, hide side panel to avoid cramped layout. Side panel reappears when form closes.
  </action>
  <verify>Run `npm run dev`, navigate to /calendar. Test: (1) side panel visible with "Next Actions" header, (2) next actions listed grouped by context, (3) completing an action from the panel works with toast+undo, (4) toggle button shows/hides panel, (5) opening EventForm hides side panel, closing form restores it.</verify>
  <done>CalendarSidePanel shows next actions grouped by context alongside the calendar. Actions can be completed from the panel. Toggle button controls visibility. Side panel hides when EventForm is open. Empty state shown when no actions exist. The calendar+side panel layout answers "what can I do now given my schedule?"</done>
</task>

</tasks>

<verification>
- ICS file import works via file input and drag-and-drop
- Invalid .ics files show error message
- Valid .ics files show preview with event count, then import on confirmation
- Imported events appear on the calendar immediately
- Recurring events expand correctly across visible date range
- Side panel shows next actions grouped by context
- Completing actions from side panel works with toast and undo
- Side panel toggles on/off
- EventForm and side panel don't overlap (mutually exclusive display)
</verification>

<success_criteria>
- User can import .ics files and see events on calendar
- Recurring events display on multiple dates via rrule expansion
- Side panel shows GTD next actions alongside calendar
- Calendar + side panel together answer "what can I do now given my schedule?"
- Toggle button controls side panel visibility
- Import provides preview before committing
</success_criteria>

<output>
After completion, create `.planning/phases/08-calendar-view/08-05-SUMMARY.md`
</output>
