---
phase: 08-calendar-view
plan: 07
type: execute
wave: 5
depends_on: ["08-04", "08-05", "08-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can view a read-only calendar showing their schedule"
    - "Calendar displays time-specific commitments separately from next actions"
    - "Calendar view helps user answer 'what can I do now given my schedule?'"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete calendar view feature.

Purpose: Verify that all calendar capabilities work end-to-end as an integrated GTD tool. The calendar must feel native to the app, display events correctly, support creation/editing/import, and help the user plan their day by showing hard landscape alongside next actions.
Output: Verified working calendar view or documented issues for gap closure.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-calendar-view/08-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Calendar View feature for Phase 8:
- Calendar with day/week/month views and navigation
- Event creation via clicking time slots or "New Event" button
- Event editing by clicking existing events
- Event deletion from edit form
- Drag-and-drop to reschedule events
- Edge dragging to resize event duration
- .ics file import with preview
- Recurring event support (daily/weekly/monthly)
- Next actions side panel alongside calendar
- Sidebar navigation link and 'c' keyboard shortcut
- ProcessingFlow "Schedule it" path from inbox to calendar
- Dark mode support throughout
  </what-built>
  <how-to-verify>
1. **Navigation**: Click "Calendar" in sidebar - should navigate to /calendar. Press 'c' key from any page - should navigate to /calendar.

2. **Calendar display**: Verify day view shows today with hourly time slots. Switch to week view - shows Mon-Sun. Switch to month view - shows full month grid. Navigate forward/backward with arrows. Click "Today" to return.

3. **Event creation**: Click an empty time slot in day or week view - EventForm should open with that time pre-filled. Enter title "Team standup", set start 9:00am, end 9:30am, pick a color, save. Event should appear as a colored time block on the calendar.

4. **Event editing**: Click the event you just created. Form opens with event data. Change title to "Team standup (daily)", set recurrence to "Daily". Save. Verify event appears on multiple days.

5. **Drag and drop**: Drag the event to a different time slot. Verify it moves. Drag the bottom edge of the event to make it longer. Verify duration changes.

6. **ICS import**: Click "Import .ics" button. Select or drag a .ics file (export one from Google Calendar or Outlook if available, or use a sample). Verify preview shows event count. Click import. Verify events appear on calendar.

7. **Side panel**: Verify "Next Actions" panel is visible alongside calendar. If you have next actions from previous phases, they should appear grouped by context. Toggle the panel off and on.

8. **Processing flow integration**: Go to Inbox. Add a new item "Doctor appointment Friday". Click process. Mark as actionable. Mark as not 2-minute. See "Schedule it" option. Click it. Enter date/time. Click "Add to Calendar". Verify item is removed from inbox and event appears on calendar.

9. **Dark mode**: Toggle dark mode. Verify calendar grid, events, forms, side panel all look correct in dark theme.

10. **Phase success criteria check**:
    - Can you view a read-only calendar showing your schedule? (YES if events display)
    - Does the calendar display time-specific commitments separately from next actions? (YES if side panel shows actions, calendar shows events)
    - Does the calendar help answer "what can I do now given my schedule?" (YES if you can see schedule gaps between events + available actions in side panel)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All Phase 8 success criteria from ROADMAP.md verified:
1. User can view a read-only calendar showing their schedule
2. Calendar displays time-specific commitments separately from next actions
3. Calendar view helps user answer "what can I do now given my schedule?"
</verification>

<success_criteria>
Human confirms all calendar features work correctly end-to-end. If issues found, they will be documented for gap closure planning.
</success_criteria>

<output>
After completion, create `.planning/phases/08-calendar-view/08-07-SUMMARY.md`
</output>
