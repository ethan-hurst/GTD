---
wave: 1
depends_on: null
files_modified:
  - "src/lib/stores/storage.svelte.ts"
autonomous: true
---

# Plan 08.2-01: Refactor Storage Persistence State Management

## 1. Objective

Refactor the `src/lib/stores/storage.svelte.ts` store to correctly manage and expose the three possible states of browser storage persistence: `'UNKNOWN'`, `'GRANTED'`, and `'DENIED'`. This provides the foundation for the UI to give clear user feedback.

## 2. Tasks

<tasks>

  <task>
    <file_path>src/lib/stores/storage.svelte.ts</file_path>
    <instruction>
Read the contents of `src/lib/stores/storage.svelte.ts` to understand its current structure before modifying it.
    </instruction>
    <read_file />
  </task>

  <task>
    <file_path>src/lib/stores/storage.svelte.ts</file_path>
    <instruction>
Modify the store to manage a three-state variable for persistence.
1. Introduce a new `$state` variable, e.g., `let persistenceState = $state<'UNKNOWN' | 'GRANTED' | 'DENIED'>('UNKNOWN');`.
2. Create and export an `async` function `checkPersistence()`. Inside this function, call `navigator.storage.persisted()`. If it returns `true`, set `persistenceState = 'GRANTED'`; otherwise, leave it as `'UNKNOWN'`. Add console logs to announce the result.
3. Create and export an `async` function `requestPersistence()`. Inside this function, call `navigator.storage.persist()`. If the promise resolves to `true`, set `persistenceState = 'GRANTED'`. If it resolves to `false`, set `persistenceState = 'DENIED'`. Add console logs for both outcomes.
4. Export the `persistenceState` and the two new functions.
    </instruction>
    <replace>
      <old_string>
// Current content of the file
      </old_string>
      <new_string>
// New, refactored content of the file
      </new_string>
    </replace>
  </task>

</tasks>

## 3. Verification

<verification>

1.  **Run the application (`npm run dev`).**
2.  **Open Browser DevTools:** Open the JavaScript console.
3.  **Initial State Check:** On page load, you should see a console log from `checkPersistence()` indicating whether storage was already persistent.
4.  **Trigger Request:** Manually call the `requestPersistence()` function from the console by typing `window.requestPersistence()` (you may need to expose it globally for testing).
5.  **Observe Logs:** Check the console for the log from `requestPersistence()` showing whether the request was `'GRANTED'` or `'DENIED'`.
6.  **State Inspection:** Use Svelte DevTools (if installed) or console commands to inspect the `persistenceState` variable in the store and confirm it matches the logged output.

</verification>

## 4. must_haves

<must_haves>

- The `storage.svelte.ts` module MUST export a reactive variable (`persistenceState`) that holds one of the three states: `'UNKNOWN'`, `'GRANTED'`, or `'DENIED'`.
- The module MUST export an `async` function `checkPersistence` that updates the state based on `navigator.storage.persisted()`.
- The module MUST export an `async` function `requestPersistence` that updates the state based on the result of `navigator.storage.persist()`.

</must_haves>
