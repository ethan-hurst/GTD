---
wave: 2
depends_on: "08.2-01"
files_modified:
  - "src/routes/settings/+page.svelte"
autonomous: true
---

# Plan 08.2-02: Integrate Persistence UI with State Store

## 1. Objective

Locate the UI component responsible for managing storage persistence and connect it to the updated `storage.svelte.ts` store. The UI must provide clear, reactive feedback to the user for all three persistence states (`'UNKNOWN'`, `'GRANTED'`, `'DENIED'`).

## 2. Tasks

<tasks>

  <task>
    <instruction>
First, locate the UI component related to storage settings. Based on the file structure, it is likely on the settings page. I will check `src/routes/settings/+page.svelte`.
    </instruction>
    <read_file>
      <file_path>src/routes/settings/+page.svelte</file_path>
    </read_file>
  </task>

  <task>
    <file_path>src/routes/settings/+page.svelte</file_path>
    <instruction>
Integrate the settings page with the `storage.svelte.ts` store.
1. Import `persistenceState`, `checkPersistence`, and `requestPersistence` from `src/lib/stores/storage.svelte.ts`.
2. In the component's script, use `onMount` from Svelte to call `checkPersistence()` when the component first loads.
3. Remove any old logic for checking/requesting persistence.
4. In the markup, create a reactive block that displays different content based on the value of `persistenceState`:
   - If `persistenceState === 'GRANTED'`, display a message like: `<p>✅ Storage is persistent.</p>`.
   - If `persistenceState === 'DENIED'`, display: `<p>❌ Persistent storage was not enabled by the browser.</p>`.
   - If `persistenceState === 'UNKNOWN'`, display a button: `<button onclick={requestPersistence}>Enable Persistent Storage</button>`.
    </instruction>
    <replace>
      <old_string>
// Old settings page content
      </old_string>
      <new_string>
// New settings page content with reactive UI
      </new_string>
    </replace>
  </task>

</tasks>

## 3. Verification

<verification>

1.  **Run the application (`npm run dev`).**
2.  **Navigate to the Settings page.**
3.  **Observe Initial State:** The UI should initially show the "Enable" button (assuming persistence is not yet granted).
4.  **Click the Button:** Click the "Enable Persistent Storage" button.
5.  **Observe UI Change:** The UI should update immediately to show either the "✅ Storage is persistent" message or the "❌ Persistent storage was not enabled" message, depending on the browser's response. This must happen without a page reload.
6.  **Refresh the Page:** After successfully enabling persistence, refresh the page. The settings page should now immediately show the "✅ Storage is persistent" message, as retrieved by `checkPersistence` on mount.

</verification>

## 4. must_haves

<must_haves>

- The settings page MUST import the state and functions from `storage.svelte.ts`.
- The `checkPersistence` function MUST be called when the component mounts.
- The UI MUST display a unique message or button for each of the three states: `'UNKNOWN'`, `'GRANTED'`, and `'DENIED'`.
- The UI's "Enable" button MUST call the `requestPersistence` function when clicked.

</must_haves>
